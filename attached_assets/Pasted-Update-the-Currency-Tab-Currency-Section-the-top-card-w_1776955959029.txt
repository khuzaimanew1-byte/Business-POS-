Update the **Currency Tab → Currency Section (the top card with USD / PKR / OMR options)** into a **USD-based value display + editable exchange system**.

This instruction specifically refers to the **second section shown in the UI (the card-style currency selector area with 3 boxes: USD, PKR, OMR)** — modify THAT section only.

---

# 🎯 TARGET SECTION (IMPORTANT CONTEXT)

You are modifying:

👉 The **Currency selection card section**

* Currently shows:

  * USD → “$ • US Dollar”
  * PKR → “Rs • Pakistani Rupee”
  * OMR → “OMR • Omani Rial”

❌ Remove name-based display
✅ Replace with **value-based system (1 USD reference)**

---

# 🔥 STRUCTURE UPDATE (VIEW MODE)

## ❌ REMOVE:

* Currency names (US Dollar, Pakistani Rupee, etc.)
* Description text inside cards

---

## ✅ REPLACE WITH VALUE DISPLAY:

Each card should now show **conversion value from 1 USD**

---

### 💵 USD CARD (LOCKED)

* Display:

  * `$ 1`
* This is base value
* Always fixed
* Highlighted as active

---

### 🇵🇰 PKR CARD

* Display:

  * Example: `₨ 280`
* Meaning:

  * 1 USD = 280 PKR

---

### 🇴🇲 OMR CARD

* Display:

  * Example: `0.38 OMR`
* “OMR” label must appear on **right side**

---

# 🔘 CHANGE BUTTON (TOP RIGHT OF THIS SECTION)

Add a **“Change” button** inside this same currency section (top-right aligned)

---

# ✏️ EDIT MODE (ON CLICK CHANGE)

When user clicks **Change**:

## Replace cards with inline inputs (same positions)

---

### 🧱 LAYOUT (STILL 3 IN ONE ROW)

```text
[ USD (locked) ]   [ PKR input ]   [ OMR input ]
```

---

## 💵 USD INPUT (FIRST)

* Value:

  * `$ 1`
* Locked (non-editable)
* Show **left-side arrow icon (→)** inside or near this field

  * This indicates USD is the source

---

## 🇵🇰 PKR INPUT (SECOND)

* Editable numeric input
* Represents:

  * value of 1 USD in PKR
* Show:

  * ₨ symbol (can be prefix or suffix, but clear)

---

## 🇴🇲 OMR INPUT (THIRD)

* Editable numeric input
* Represents:

  * value of 1 USD in OMR
* “OMR” label must be on **right side inside input**

---

# 🔁 ARROW LOGIC (IMPORTANT)

* Only ONE directional indication:

  * From USD → others

✔ Show arrow near USD
❌ Do NOT show arrow between PKR and OMR

---

# 💾 BUTTON BEHAVIOR

## SAME BUTTON:

### View mode:

* Label: **Change**

### Edit mode:

* Label: **Save**

---

## On Save:

* Apply new rates globally
* Update all system values:

  * Product prices
  * Cart totals
  * Analytics
  * Add Product page

No reload required

---

# 🎨 UI RULES

* Maintain **same card alignment**
* No layout shift when switching modes
* Smooth transition (fade or inline morph)
* Keep design minimal and premium

---

# 🚫 CONSTRAINTS

* Do NOT reintroduce currency names
* Do NOT allow USD editing
* Do NOT create new section — modify existing one only
* Keep everything inside this currency card section

---

# 🎯 FINAL RESULT

This section becomes:

> “A visual currency selector + live USD-based conversion controller”

Where:

* User sees real conversion values
* Can edit them inline
* System updates globally from a single source (USD)
